# Summary

Mx is the top folder populated by STM32CubeMX tool, like this :

    <ProjectName>/kernel
    <ProjectName>/u-boot
    <ProjectName>/tf-a

Then the specific machine conf/machine/stm32mp1-mx.conf must be updated in accordance

# Assign CubeMX Board DT and project name
CUBEMX_DTB = "stm32mp157c-<ProjectName>-mx"
CUBEMX_PROJECT = "<ProjectName>"
